// 各 nodeId 的专用 normalize 预设（方案 C）。
// 通过导出常量复用，避免在多个调用站点重复定义 schema。

import type { NormalizeConfig } from './normalizer';

/* ─── screenplay.7 / adapt.6：剧本格式化 ─────────────────────────── */
export const SCREENPLAY_FINAL_NORMALIZE: NormalizeConfig = {
  schema: `输出为「中文短剧场次剧本（markdown）」，遵循以下结构：

# 剧名

## 第N集

### 场1 — 场景名（INT./EXT. · 时间 · 地点）

【场景描述】1-2 行，给出关键道具和氛围。

人物A：（动作/语气）台词台词。
人物B：（动作/语气）台词。

【动作/分镜提示】不超过 2 行，写关键画面或运镜要点。

----

### 场2 — …

规则：
- 场次以 "### 场N — 名" 开头，编号连续。
- 角色名后接中文冒号「：」，括号内为表演提示。
- 不要使用「FADE IN/OUT」「CUT TO」等英文术语；统一用中文。
- 保留原文叙事顺序与人物名；只做格式化，不删改剧情。
- 若原文是小说叙述，提取场景与对话改写为剧本格式；旁白以【画外音】标注。`,
  examples: `# 示例

## 第1集

### 场1 — 雨夜地下车库（INT. · 23:30 · 地下停车场B2）

【场景描述】惨白灯光，水迹反光，远处一辆黑色SUV停下。

林夏：（攥紧背包带）真的非要现在见？
陈默：（推开车门，目光锐利）再晚就来不及了。

【动作/分镜提示】特写林夏发抖的手；切SUV后视镜里的人影。`,
  maxTokens: 12000,
};

/* ─── assets.4：资产清单 JSON ────────────────────────────────────── */
export const ASSETS_LIST_NORMALIZE: NormalizeConfig = {
  schema: `输出为合法 JSON 数组，每个元素描述一个资产/道具/场景物件：
[
  {
    "id": "kebab-case-id",
    "name": "中文名称",
    "category": "道具" | "场景" | "服化" | "特殊视效",
    "desc": "外观描述（1-2 句，含材质/颜色/尺寸）",
    "scenes": ["第1集场1", "第1集场3"]
  }
]

规则：
- 顶层必须是 [ … ] 数组；不要包对象、不要前后说明文字。
- 字符串使用双引号；最后一个元素后不要尾随逗号。
- 没有的字段宁可留空字符串 "" 或空数组 []，不要捏造。
- 若原文是 markdown 列表 / 自然语言，逐条抽取为数组元素。`,
  examples: `[
  {
    "id": "black-suv",
    "name": "黑色SUV",
    "category": "道具",
    "desc": "进口大型SUV，车窗贴深色膜，车牌模糊。",
    "scenes": ["第1集场1"]
  }
]`,
  maxTokens: 6000,
};

